import { MobilePipePipe } from './mobile-pipe.pipe';

describe('MobilePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MobilePipePipe();
    expect(pipe).toBeTruthy();
  });
});
